# Credenciales — NUNCA subir
.env
.env.local
.env.*
!.env.example
*api-key*
*api_key*
*.credentials

# .mcp.json AHORA es seguro versionar: usa ${VAR} y los secretos
# viven en .env (ignorado). Se versiona para que el template quede listo.

# Workflows personales — no se comparten (tienen IDs de credenciales específicos)
workflows/*.json
!workflows/starters/*.json

# macOS
.DS_Store

# Node
node_modules/
npm-debug.log*

# Claude Code — cache local (no relevante para el repo)
.claude/cache/
.claude/history.jsonl
.claude/sessions/

# Playwright artifacts
test-results/
playwright-report/
screenshots/*.png

# Logs y temporales
*.log
*.tmp
